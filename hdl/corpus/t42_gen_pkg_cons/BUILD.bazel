# SPDX-License-Identifier: Apache-2.0

load("//build:wdb_case.bzl", "wdb_case")

package(default_visibility = ["//visibility:public"])

wdb_case(
    name = "t42_gen_pkg_cons",
    # Compilation order matters to xvhdl; do not sort.
    # do not sort
    srcs = [
        "gp.pkg.vhdl",
        "tb.ent.vhdl",
    ],
)
