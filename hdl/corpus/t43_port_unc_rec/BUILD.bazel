# SPDX-License-Identifier: Apache-2.0

load("//build:wdb_case.bzl", "wdb_case")

package(default_visibility = ["//visibility:public"])

wdb_case(
    name = "t43_port_unc_rec",
    # Compilation order matters to xvhdl; do not sort.
    # do not sort
    srcs = [
        "bundle.pkg.vhdl",
        "child.ent.vhdl",
        "tb.ent.vhdl",
    ],
)
