# SPDX-License-Identifier: Apache-2.0

load("//build:wdb_case.bzl", "wdb_case")

package(default_visibility = ["//visibility:public"])

wdb_case(
    name = "t73_prt_arch_gen",
    # Compilation order matters to xvhdl; do not sort.
    # do not sort
    srcs = [
        "tb.ent.vhdl",
    ],
    xelab_args = [
        "-debug",
        "typical",
        "-debug",
        "subprogram",
    ],
)
