# SPDX-License-Identifier: Apache-2.0

load("//build:wdb_case.bzl", "wdb_case")

package(default_visibility = ["//visibility:public"])

wdb_case(
    name = "t34_port_fld_out",
    # Compilation order matters to xvhdl; do not sort.
    # do not sort
    srcs = [
        "trio.pkg.vhdl",
        "child.ent.vhdl",
        "tb.ent.vhdl",
    ],
)
