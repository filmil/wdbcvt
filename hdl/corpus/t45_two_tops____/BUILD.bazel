# SPDX-License-Identifier: Apache-2.0

load("//build:wdb_case.bzl", "wdb_case")

package(default_visibility = ["//visibility:public"])

wdb_case(
    name = "t45_two_tops____",
    # Compilation order matters to xvhdl; do not sort.
    # do not sort
    srcs = [
        "tb2.ent.vhdl",
        "tb.ent.vhdl",
    ],
    xelab_args = [
        "--top",
        "corpus.tb2",
    ],
)
