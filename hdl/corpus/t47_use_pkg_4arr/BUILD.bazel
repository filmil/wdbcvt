# SPDX-License-Identifier: Apache-2.0

load("//build:wdb_case.bzl", "wdb_case")

package(default_visibility = ["//visibility:public"])

wdb_case(
    name = "t47_use_pkg_4arr",
    # Compilation order matters to xvhdl; do not sort.
    # do not sort
    srcs = [
        "arr.pkg.vhdl",
        "tb.ent.vhdl",
    ],
)
