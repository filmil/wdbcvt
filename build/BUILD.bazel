# SPDX-License-Identifier: Apache-2.0

load("@bazel_skylib//:bzl_library.bzl", "bzl_library")

package(default_visibility = ["//visibility:public"])

bzl_library(
    name = "wdb_case",
    srcs = ["wdb_case.bzl"],
    deps = [
        ":fst_test",
        "@rules_vivado//build/vivado:rules",
    ],
)

bzl_library(
    name = "fst_test",
    # keep: gazelle's bzl plugin drops this attribute, and the library
    # is empty without it.
    srcs = ["fst_test.bzl"],
    deps = ["@rules_shell//shell:rules_bzl"],
)

exports_files(["fst_convert_test.sh"])
